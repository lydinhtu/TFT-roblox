-- Script: AI_Enemy (Bỏ trong model QuaiVat)
local ServerScriptService = game:GetService("ServerScriptService")
local UnitAI = require(ServerScriptService:WaitForChild("UnitAI"))

-- Cấu hình cho Quái Vật
local config = {
	IsRanged = false, -- Chỉnh thành true nếu là quái bắn xa
	AnimationID = "rbxassetid://507777826" -- ID animation tấn công
}

-- Kích hoạt bộ não chung (Giống LinhTa)
UnitAI.KhoiTao(script.Parent, config)
